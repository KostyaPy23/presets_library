{
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0",
    "cors": "^2.7.1",
    "express": "^4.13.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server -a localhost -p 8000 -c-1 ./"
  }
}